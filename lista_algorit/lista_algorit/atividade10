#include <stdio.h>

int main() {
   int num, soma = 0;
   float media;
   int i;

   printf("Escreva 10 numeros inteiros:\n");

   for(i = 0; i < 10; i++) {
      scanf("%d", &num);
      soma += num;
   }

   media = soma / 10.0;
   printf("A media dos numeros é %.2f\n", media);

   return 0;
}
